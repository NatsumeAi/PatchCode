Call the `review` tool to run an isolated code review. Do not start a second drain or spawn a parallel reviewer yourself — `review` is the only review execution path.

Map $ARGUMENTS as follows:
- empty → `review` with default scope (uncommitted diff)
- file paths → `review` with scope "paths"
- otherwise pass the argument text in the user-visible summary and still call `review`

Then present the tool JSON (findings + verdict) to the user.

---

Input: $ARGUMENTS
