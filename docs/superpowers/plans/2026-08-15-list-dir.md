# list_dir Implementation Plan (W8a)

> REQUIRED: subagent-driven-development or executing-plans.

**Goal:** Ship `docs/superpowers/specs/2026-08-15-list-dir-design.md`.

## Files

- Create: `packages/core/src/tool/list-dir.ts`
- Modify: `packages/core/src/tool/builtins.ts`
- Create: `packages/core/test/tool-list-dir.test.ts` (clone `tool-read.test.ts` harness)

### Task 1

- [ ] Failing test: definitions contain `list_dir`; live tmp listing; spawn inventory (`rg spawn list-dir.ts` empty).
- [ ] Implement using `ReadToolFileSystem` list path (extract shared `listDirectory` if needed rather than copy-paste 200 lines).
- [ ] Permission `read`; external_directory same as read.
- [ ] `cd packages/core && bun test --timeout 60000 test/tool-list-dir.test.ts test/tool-read.test.ts`

## Done

Spec 1–5. Reviewer: no bash, same paging as read.

## Out

- Changing `read` directory mode
- gitignore-only listings
- Shelling out to `ls`
