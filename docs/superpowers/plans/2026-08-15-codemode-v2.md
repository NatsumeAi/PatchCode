# CodeMode V2 Implementation Plan (W8d)

> REQUIRED: subagent-driven-development or executing-plans.

**Goal:** Ship `docs/superpowers/specs/2026-08-15-codemode-v2-design.md`.

## Files

- Create: `packages/core/src/tool/execute.ts` (port host wiring from opencode code-mode.ts; depend on `@opencode-ai/codemode`)
- Modify: `packages/core/package.json` workspace dep
- Modify: `builtins.ts`
- Modify: opencode `tool/registry.ts` — stop exclusive experimental registration
- Port tests from `packages/opencode/test/tool/code-mode.test.ts` to `packages/core/test/tool-execute.test.ts`

### Task 1: core tool

- [ ] Definitions include `execute` with default Location graph (no env flag).
- [ ] Script `return 1+1` → output 2.

### Task 2: nested settle

- [ ] Register dummy `ping`; script `await tools.ping({})`; ping execute count 1; settle used (spy).
- [ ] Permission deny on ping → execute ToolFailure.

### Task 3: retire V1-only gate

- [ ] Remove `experimentalCodeMode` requirement for advertising `execute`.
- [ ] **Delete** `packages/opencode/src/tool/code-mode.ts` from the V1 registry. Tests import core `execute`. Do **not** leave `code-mode.ts` as a second advertiser/wrapper.
- [ ] Run `packages/core` execute tests + move/update `packages/opencode/test/tool/code-mode.test.ts` to core.

## Done

Spec 1–5. Reviewer: no flag needed; inner tools go through settle.

## Out

- V8 / Codex code-mode
- New stdlib beyond current CodeMode
